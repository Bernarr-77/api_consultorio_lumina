version: '3.8'
services:
    postgres:
        image: postgres:17.0
        env_file:
            - .env
        ports:
            - 5432:5432
        volumes:
            - dados:/var/lib/postgresql/data
    redis:
        image: redis:8-alpine

        ports:
            - 6379:6379
volumes:
    dados: